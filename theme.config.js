const colors = require('./config/colors')

module.exports = {
  name: 'Early Riser',
  inputFile: 'config/theme.json',
  outputDir: 'themes',
  colors,
  editorBackground: '#f7fafc',
  borderColor: '#d3dbe4',
}
